using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameManager : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    //Need method to open door when enemy count is 0
    //Need method to restart scene when player walks into door
    //Need method to end game is player dies
    //Keep track of score

    // Update is called once per frame
    void Update()
    {
        
    }
}
